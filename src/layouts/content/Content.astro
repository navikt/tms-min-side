---
import styles from "./Content.module.css";

export interface Props {
  cls?: keyof typeof styles;
}

const { cls = "min-side" } = Astro.props;
---

<div class={styles[cls]}>
  <div class={styles.layout}>
    <main id="maincontent">
      <slot />
    </main>
  </div>
</div>
